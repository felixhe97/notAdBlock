Installation and Usage:

Requires relatively recent versions of CMake, Make, and g++ to build.

Developed on Ubuntu 18.04 LTS, should be portable to all OS with relevant build
tools.

In terminal, type in "cmake CMakeLists.txt", then type in "make". Once
executable is built, type "./filters" to run.
